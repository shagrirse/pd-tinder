# Generated by scripts/deploy.sh into deploy/.env on the instance; this
# template is the documentation of record. Every value here is non-secret
# (no secrets exist in this deployment).
DATABASE_URL=/data/pd-tinder.db
ORIGIN=https://pdtinder.example.com
BODY_SIZE_LIMIT=5M
PORT=3000
SITE_DOMAIN=pdtinder.example.com
LITESTREAM_BUCKET=pdtinder-backup
APP_IMAGE=<ecr-repository-uri>:<tag>
